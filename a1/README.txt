You can run the file in the typical way, but you can also use 
--timeout instead of -t,
--maxrepeat instead of -r and 
--port instead of -p.
You can also do dnsClient.py -h for a list of all arguments.
